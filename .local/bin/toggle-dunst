#!/bin/bash

if [[ $(dunstctl is-paused) == false ]]; then

  echo "dunst is enabled here"
  dunstify "Disabling dunst"
  sleep 2
  dunstctl close-all
fi

dunstctl set-paused toggle

if [[ $(dunstctl is-paused) == false ]]; then

  echo "dunst is enabled there"
  dunstify "Enabling dunst"
fi
